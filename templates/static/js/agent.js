(()=>{const form=document.getElementById('chat-form'),input=document.getElementById('chat-input'),box=document.getElementById('messages');if(!form)return;let cid=localStorage.getItem('novahr_conversation_id')||'';const toast=t=>{const r=document.getElementById('toast-root');if(!r)return;const e=document.createElement('div');e.className='toast';e.textContent=t;r.appendChild(e);setTimeout(()=>e.remove(),3200)};const bubble=(who,text,kind)=>{const d=document.createElement('div');d.className='bubble '+kind;d.innerHTML=`<b>${who}</b><p></p>`;d.querySelector('p').textContent=text;box.appendChild(d);box.scrollTop=box.scrollHeight};async function send(message){message=(message||input.value).trim();if(!message)return;bubble('You',message,'user');input.value='';const typing=document.createElement('div');typing.className='bubble bot';typing.innerHTML='<b>✦ Nova</b><p>Thinking through your request…</p>';box.appendChild(typing);box.scrollTop=box.scrollHeight;try{const res=await fetch('/agent/api/',{method:'POST',headers:{'Content-Type':'application/json','X-CSRFToken':document.cookie.match(/csrftoken=([^;]+)/)?.[1]||''},body:JSON.stringify({message,conversation_id:cid||null})});const data=await res.json();typing.remove();if(data.conversation_id){cid=data.conversation_id;localStorage.setItem('novahr_conversation_id',cid)}bubble('✦ Nova',data.message||'No response received.','bot');if(data.status==='denied')toast('Permission protected: request was not executed.')}catch(e){typing.remove();bubble('✦ Nova','Nova is temporarily unavailable. Please check the Django server.','bot');toast('Connection error')}}form.addEventListener('submit',e=>{e.preventDefault();send()});document.querySelectorAll('.suggestions button').forEach(b=>b.onclick=()=>send(b.textContent));})();
